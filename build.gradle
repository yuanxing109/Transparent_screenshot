buildscript {
    repositories {
        google()
        mavenCentral()
        // 删除了不完整的 maven { url 'https: 行
    }
}

plugins {
    id 'com.android.application' version '8.13.2' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
